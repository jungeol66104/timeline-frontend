import React from 'react';

const TimelineEventsBottom = ({isEnd}:{isEnd: boolean}) => {
    return (
        <div className={'timelineEventsBottom flex items-center h-[60px] mt-2.5'} style={{zIndex: 4000}}>
        </div>
    );
};

export default TimelineEventsBottom;
