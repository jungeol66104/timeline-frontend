import React from 'react';
import ShareButtonTemplate from "@/components/layout/share/shareButtonTemplate";

const XButton = () => {
    return (
        <ShareButtonTemplate svgPath={'/svg/x.svg'} title={'X'} size={18}/>
    );
};

export default XButton;
